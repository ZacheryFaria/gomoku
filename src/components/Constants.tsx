export const boardSquares = 18;
export const boardSize = 4000;
export const cellSize = Math.floor(boardSize / (boardSquares+1));
export const boardMargin = (boardSize - (cellSize * boardSquares)) / 2;

export const cellColor = "#e5b773"
